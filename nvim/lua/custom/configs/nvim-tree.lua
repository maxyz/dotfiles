local M = {
  git = {
    enable = true,
    ignore = false,
  },
  update_focused_file = {
    update_cwd = true,
  },
  actions = {
    open_file = {
      window_picker = {
        enable = false,
      },
    },
  },
}

return M
